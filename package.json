{
  "name": "divcalc",
  "version": "2.0.0-alpha.0",
  "private": true,
  "description": "The Division 2 build calculator — divcalc.xyz",
  "type": "module",
  "homepage": "https://divcalc.xyz",
  "scripts": {
    "dev": "pnpm --filter @divcalc/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "packageManager": "pnpm@10.33.1",
  "engines": {
    "node": ">=20"
  }
}
