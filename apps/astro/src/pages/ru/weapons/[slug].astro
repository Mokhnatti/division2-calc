---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { WEAPONS, slugFromId, weaponName, weaponCategoryLabel, type Weapon } from '../../../data/weapon-helpers';

export async function getStaticPaths() {
  return WEAPONS.map((w) => ({
    params: { slug: slugFromId(w.id) },
    props: { weapon: w },
  }));
}

interface Props { weapon: Weapon }
const { weapon: w } = Astro.props;
const name = weaponName(w, 'ru');
const cat = weaponCategoryLabel(w.category, 'ru');
const kindLabel = w.kind === 'exotic' ? 'Экзотик' : w.kind === 'named' ? 'Именное' : 'Базовое';

const title = `${name} — ${kindLabel} ${cat} билд и статы | Division 2 калькулятор`;
const description = `${name}: ${w.baseDamage.toLocaleString()} базовый урон, ${w.rpm} В/М, магазин ${w.magazine}, перезарядка ${w.reloadSeconds}с. Топ-билды, талант, DPS калькулятор.`;
const canonical = `https://divcalc.xyz/ru/weapons/${slugFromId(w.id)}`;
---
<BaseLayout {title} {description} {canonical}>
  <main class="weapon-page">
    <nav class="breadcrumb">
      <a href="/ru/">Главная</a> · <a href="/ru/weapons/">Оружие</a> · <span>{name}</span>
    </nav>
    <header>
      <span class={`kind-badge kind-${w.kind}`}>{kindLabel}</span>
      <span class="cat-badge">{cat}</span>
      <h1>{name}</h1>
    </header>
    <section class="stats-grid">
      <div class="stat"><div class="v">{w.baseDamage.toLocaleString()}</div><div class="l">Базовый урон</div></div>
      <div class="stat"><div class="v">{w.rpm}</div><div class="l">В/М</div></div>
      <div class="stat"><div class="v">{w.magazine}</div><div class="l">Магазин</div></div>
      <div class="stat"><div class="v">{w.reloadSeconds}с</div><div class="l">Перезарядка (пустой)</div></div>
      {w.optimalRange && (<div class="stat"><div class="v">{w.optimalRange}м</div><div class="l">Опт. дальность</div></div>)}
      {w.headshotMultiplier && (<div class="stat"><div class="v">×{w.headshotMultiplier}</div><div class="l">Множитель хедшота</div></div>)}
    </section>

    {w.intrinsicAttrs && w.intrinsicAttrs.length > 0 && (
      <section class="intrinsic">
        <h2>Встроенные атрибуты</h2>
        <p class="muted">Постоянные бонусы оружия:</p>
        <ul>
          {w.intrinsicAttrs.map(a => (
            <li><strong>+{a.value}%</strong> {a.stat.toUpperCase()}{a.max && <span class="dim"> (макс. ролл: +{a.max}%)</span>}</li>
          ))}
        </ul>
      </section>
    )}

    {w.builtInMods && w.builtInMods.length > 0 && (
      <section class="built-in-mods">
        <h2>Встроенные обвесы (экзотик)</h2>
        <p class="muted">Слоты обвесов экзотика заблокированы под эти модули:</p>
        <table>
          <thead><tr><th>Слот</th><th>Обвес</th><th>Бонус</th></tr></thead>
          <tbody>
            {w.builtInMods.map(m => (
              <tr>
                <td>{m.slot}</td>
                <td>{m.name_ru || m.stat}</td>
                <td>+{m.value}{m.stat === 'mag_add' ? ' патронов' : '%'} {m.stat === 'mag_add' ? '' : m.stat.toUpperCase()}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </section>
    )}

    {w.appliesBurn && (
      <section class="burn-info">
        <h2>🔥 Поджигает цель</h2>
        <p>Оружие накладывает <strong>горение</strong> на цель. Урон от горения зависит от <strong>Негативные эффекты %</strong> и <strong>Длительность горения %</strong> — независимо от урона оружия.</p>
        <p>Базовая формула: <code>300 × 40 × (1 + Status%) ≈ 12,000 урона/тик × длительность (5с базово)</code></p>
      </section>
    )}

    <section class="cta">
      <h2>Открыть в калькуляторе</h2>
      <p>Открыть DPS калькулятор с этим оружием:</p>
      <a class="btn" href={`/spa/index.html#build?w=${slugFromId(w.id)}`}>⚙ Открыть в калькуляторе</a>
    </section>

    <footer class="page-footer">
      <p class="dim">TU22.1 "Rise Up" · Проверено: 2026-04-25 · 1:1 с игрой</p>
    </footer>
  </main>
</BaseLayout>

<style>
  .weapon-page { max-width: 900px; margin: 0 auto; padding: 30px 20px; }
  .breadcrumb { font-size: 12px; color: var(--muted); margin-bottom: 14px; }
  .breadcrumb a { color: var(--muted); }
  header { margin-bottom: 24px; }
  .kind-badge, .cat-badge {
    display: inline-block; font-size: 10px; font-weight: 700; letter-spacing: .12em;
    text-transform: uppercase; padding: 3px 8px; border-radius: 4px; margin-right: 6px;
  }
  .kind-base { background: rgba(96,128,160,.2); color: #a4b5c8; }
  .kind-named { background: rgba(254,175,16,.2); color: var(--orange); }
  .kind-exotic { background: rgba(239,83,80,.25); color: #ef5350; }
  .cat-badge { background: var(--bg-2); color: var(--muted); border: 1px solid var(--border); }
  h1 { margin: 8px 0 0; font-size: 32px; }
  .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 8px; margin-bottom: 24px; }
  .stat { padding: 12px; background: var(--bg-2); border: 1px solid var(--border); border-radius: 6px; text-align: center; }
  .stat .v { font-size: 22px; font-weight: 700; }
  .stat .l { font-size: 10px; font-weight: 700; letter-spacing: .12em; color: var(--muted); margin-top: 4px; text-transform: uppercase; }
  section { margin-bottom: 28px; }
  section h2 { font-size: 18px; margin: 0 0 10px; color: var(--orange); }
  .muted { color: var(--text-dim); font-size: 13px; }
  .dim { color: var(--muted); font-size: 11px; }
  table { width: 100%; border-collapse: collapse; margin-top: 8px; }
  th, td { padding: 8px 10px; text-align: left; border-bottom: 1px solid var(--border); font-size: 13px; }
  th { color: var(--muted); font-weight: 700; font-size: 10px; letter-spacing: .12em; text-transform: uppercase; }
  .burn-info { background: rgba(255,101,51,.05); border-left: 3px solid #ff6533; padding: 14px 18px; border-radius: 6px; }
  .burn-info code { background: rgba(0,0,0,.3); padding: 2px 6px; border-radius: 3px; font-size: 12px; }
  .cta { padding: 18px; background: var(--bg-2); border: 1px solid rgba(254,175,16,.3); border-radius: 6px; }
  .btn { display: inline-block; padding: 10px 20px; background: var(--orange); color: #000; font-weight: 700; border-radius: 5px; text-decoration: none; }
  .page-footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid var(--border); }
</style>
